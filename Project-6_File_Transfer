import http.server
import socketserver
PORT = 7000
handler = http.server.SimpleHTTPRequestHandler
with socketserver.TCPServer(("",PORT),handler) as http:
    print("Server runing or port", PORT)
    http.serve_forever()
